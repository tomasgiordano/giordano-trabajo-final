import { SortTurnosPipe } from './sort-turnos.pipe';

describe('SortTurnosPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTurnosPipe();
    expect(pipe).toBeTruthy();
  });
});
