import { CalificacionPipe } from './calificacion.pipe';

describe('CalificacionPipe', () => {
  it('create an instance', () => {
    const pipe = new CalificacionPipe();
    expect(pipe).toBeTruthy();
  });
});
